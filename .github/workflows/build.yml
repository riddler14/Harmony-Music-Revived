name: Build and Release Harmony APK

on:
  push:
    branches:
      - main # Change this to 'master' if your default branch is named master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.1' # Match your local Flutter version!
          
      # 🟢 THE MAGIC: Recreate the Keystore from GitHub Secrets 🟢
      - name: Decode Keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/harmony-key.jks
        
      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=upload" >> android/key.properties
          echo "storeFile=../harmony-key.jks" >> android/key.properties

      - name: Get Dependencies
        run: flutter pub get
        
      - name: Build Signed APK
        run: flutter build apk --release
        
      # 🟢 AUTO-PUBLISH TO GITHUB RELEASES (For Obtainium) 🟢
      - name: Create Release and Upload APK
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/flutter-apk/app-release.apk"
          tag: "v${{ github.run_number }}" 
          name: "Harmony Revived Build ${{ github.run_number }}"
          body: "Automated signed build via GitHub Actions."
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true